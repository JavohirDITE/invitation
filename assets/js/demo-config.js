/* ============================================================
   НАСТРОЙКА ОТПРАВКИ В TELEGRAM (для конструктора палитры)
   ============================================================

   Чтобы кнопка «Отправить в Telegram» работала, нужно создать
   простого бота и узнать свой chat_id:

   1. В Telegram напишите боту @BotFather  →  /newbot  →  получите ТОКЕН.
   2. Напишите своему новому боту любое сообщение (например «привет»).
   3. Узнайте свой chat_id: откройте в браузере
      https://api.telegram.org/bot<ТОКЕН>/getUpdates
      и найдите "chat":{"id": ВАШ_ЧИСЛОВОЙ_ID ...}
   4. Впишите значения ниже.

   ВНИМАНИЕ: токен будет виден в коде сайта (это статический хостинг).
   Для пригласительного это не критично — бот лишь шлёт вам сообщения.
   Если не хотите светить токен — оставьте поля пустыми: тогда кнопка
   скопирует палитру и откроет ваш чат в Telegram, где её можно вставить.
   ============================================================ */
window.TG = {
  botToken: "",      // например "1234567890:AAExxxxxxxxxxxxxxxxxxxxxxxxxx"
  chatId: "",        // например "123456789"

  // Запасной вариант (если бот не настроен): ваш ник в Telegram без @
  fallbackUsername: ""  // например "username"
};
